// Removed: the just_run tool (ADR 0003) is gone now that recipe_run
// (ADR 0004/0005) covers the same need. Delete this file — it's no
// longer referenced from mod.rs; kept only because this tool can't
// delete files.
